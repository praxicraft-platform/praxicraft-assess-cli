# Legacy Go CLI

The interactive Assess CLI was rewritten in Bun + TypeScript (OpenTUI) in the repo root.

This directory archives the previous Go sources for reference. Do not build or release from here.
